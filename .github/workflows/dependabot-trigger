# Check for outdated dependencies on every push.
# name: Trigger Dependabot on Push

# on:
#  push:
#    branches: [ "main" ]

# jobs:
#  dependabot-trigger:
#    runs-on: ubuntu-latest

#    steps:
#      - uses: actions/checkout@v4
#      - name: Trigger Dependabot Update
#        run: |
#          echo "Triggering Dependabot updates"
          # Use GitHub API to trigger a workflow dispatch for a manual Dependabot update.
#          curl -X POST https://api.github.com/repos/${{ github.repository }}/actions/workflows/dependabot.yml/dispatches \
#            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
#            -d '{"event_type": "trigger-dependabot"}'

 # dependabot-update:
 #   runs-on: ubuntu-latest
 #   needs: dependabot-trigger

 #   steps:
 #     - uses: actions/checkout@v4
 #     - name: Running Dependabot
 #       run: |
 #         echo "Running Dependabot to update dependencies"
 #         dependabot update --all
          
